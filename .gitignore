vendor/
node_modules/
.DS_Store

# Agent scratch. .claude/worktrees/ holds linked worktrees created inside the
# repo; .superpowers/ holds subagent-driven-development ledgers, task briefs and
# review packages. Both are working state, never history.
.claude/
.superpowers/
